function soma (n1=0, n2=0) {//se não for passado ele vale 0
    var soma = `O resultado de ${n1} + ${n2} é ${n1 + n2}`
    return soma
}

//var res = soma(7, 9)
//console.log(res)

console.log(soma(6,9))